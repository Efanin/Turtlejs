var x = 5 //объявление числовой переменной
var s = 'hello world' //объявление строковой переменной
var arr = [1,'hello',true,-8,5.2] //объявление массива

if x < 10 //условие
    print(s)

for 5 //цикл повтрить 5 раз
    x+=1
    if x%2==0 // проверка на четность
        print(x)

foreach arr in element //цикл перебрать массив
    print(element)

while num > 0 //цикл пока число больше нуля
    num = input()

func my_func(a,b,c)// создание функции
    if a>b
        c=a
    else
        c=b
    return c

var num1 = my_func(1,2,3)//вызов функции

var a = 1+2  //сложение
var b = 20/5 //деление
var c = 2*4  //умножение
var d = 1-5  //вычитание
var f = 2^3  //возведение в степень
var e = 15%2 //остаток от деления
var g = 25~2 //корень из числа

var arr2
arr2.add(a,b,c,d,e,f,g) // добавление елементов в массив

arr2.foreach(x => print(x)) // вывод элементов с помощью лямда выражения
arr2.max(x => print(x))  // вывод максимального элемента с помощью лямда выражения
arr2.min(x => print(x))  // вывод минимального элемента с помощью лямда выражения
arr2.sum(x => print(x)) //вывод суммы элементов с помощью лямда выражения